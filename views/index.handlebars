<div class="center title">
<h1>Burger Bonanza!</h1>
</div>


<div class="row middle">
  <div class="col center" id="notDevoured">
    <h2>Burgers to be eaten!</h2>
    {{burgers.id}}{{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
  </div>

  <div class="col center" id="Devoured">
    <h2>Burgers that you ate!</h2>
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
  </div>
</div>

<div class="center bottom">
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bg">Burger Name:</label>
    <input type="text" id="bg" name="name">
  </div>


  <button class="btn btn-light no-pad" type="submit">Add Burger</button>
</form>
</div>